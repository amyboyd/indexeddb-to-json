{
  "name": "indexeddb-to-json",
  "description": "Extract IndexedDB to JSON, from any Chrome or Electron database directory",
  "version": "0.0.4",
  "main": "src/cli.js",
  "scripts": {
    "lint-fix": "prettier --write './*.js'; eslint ./*.js .eslintrc.js --fix",
    "lint-check": "prettier --check './*.js'; eslint ./*.js .eslintrc.js",
    "lf": "yarn lint-fix",
    "lc": "yarn lint-check"
  },
  "dependencies": {
    "commander": "^6.0.0",
    "csv-writer": "^1.6.0",
    "get-folder-size": "^2.0.1",
    "glob": "^7.1.6",
    "puppeteer": "^2.1.1",
    "recursive-copy": "^2.0.10"
  },
  "devDependencies": {
    "eslint": "^7.7.0",
    "prettier": "^2.0.5"
  },
  "engines": {
    "node": ">=13.3.0"
  },
  "files": [
    "cli/",
    "yarn.lock",
    "README.md"
  ]
}
